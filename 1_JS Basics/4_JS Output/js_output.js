//To access an HTML element, JavaScript can use the document.getElementById(id) method.
//The id attribute defines the HTML element. The innerHTML property defines the HTML content:
function Changehtml(){
    document.getElementById("demo").innerHTML="<h2>Changing the innerHTML property of an HTML element is a common way to display data in HTML</h2>";
}

function mainhtml(){
    document.getElementById("demo").innerHTML="To access an HTML element, JavaScript can use the document.getElementById(id) method.";
}